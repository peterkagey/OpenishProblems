\documentclass{article}
\include{header}
\usetikzlibrary{decorations.pathreplacing}

\begin{document}

\rating{2}{2}
Choose a point $p$ in $\mathbb{R}^2$, and consider all balls $\mathcal B_p(r)$
of radius $r$ centered at $p$. Let $f(\mathcal B_p(r))$ be the number of ``boxes'' of
$\mathbb{Z}^2$ that are partly contained in the interior of a ball.
\begin{figure}[ht!]
  \centering
  \begin{tikzpicture}
    \draw[opacity=0] (-3,-3.5) grid (3,3.5);
    \fill[cyan!40] (0,0) circle (2.95);
    \fill[magenta!40] (0,0) circle (2.5);
    \fill[yellow!40] (0,0) circle (2.1);
    \fill[green!40] (0,0) circle (1.8);
    \fill[red!40] (0,0) circle (1.2);
    \fill[blue!40] (0,0) circle (0.5);
    \draw[opacity=0.5] (-3,-3) grid (3,3);
  \end{tikzpicture}
  ~
  \begin{tikzpicture}
    \fill[cyan!50!green!40] (0,0.5) circle (2.8);
    \fill[cyan!50!blue!40] (0,0.5) circle (2.6);
    \fill[magenta!50!red!40] (0,0.5) circle (2.4);
    \fill[red!50!blue!40] (0,0.5) circle (2.03);
    \fill[cyan!40] (0,0.5) circle (1.9);
    \fill[magenta!40] (0,0.5) circle (1.6);
    \fill[yellow!40] (0,0.5) circle (1.4);
    \fill[green!40] (0,0.5) circle (1.05);
    \fill[red!40] (0,0.5) circle (0.8);
    \fill[blue!40] (0,0.5) circle (0.4);
    \draw[opacity=0.5] (-3,-3) grid (3,4);
  \end{tikzpicture}
  \caption{
    When $p \in \mathbb{Z}^2$, the range of $f$ is $\{0,4,12,16,24,32,36,\dots\}$;
    when $p$ is in the middle of an edge, the range of $f$ is $\{0,2,6,8,12,16,20,22,26,34,38,..\}$;
    when $p$ has irrational coordinates, the range of $f$ is $\mathbb N$.
  }
\end{figure}

\begin{question}
  What are all possible sequences for varying $p$?
\end{question}

\begin{related}
  \item What if $f'$ counts the number of vertices in a circle?
    Or if $f''$ counts the number of boxes that are \textit{entirely} inside
    of a circle?
  \item What happens on other lattices, tilings, or higher dimensional analogs?
  \item How does this vary when the ``circles'' are generated by other metrics?
\end{related}

\begin{references}
  \item Problem 30.
\end{references}
\end{document}
